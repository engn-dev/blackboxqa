{
  "name": "@blackboxqa/protocol",
  "version": "0.4.4",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "BlackboxQA daemon IPC protocol — Zod schemas and inferred types (single source of truth).",
  "exports": {
    ".": {
      "source": "./src/index.ts",
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "compile": "tsc --noEmit",
    "test": "echo \"(no tests for @blackboxqa/protocol)\""
  },
  "dependencies": {
    "zod": "3.25.76"
  },
  "devDependencies": {
    "@blackboxqa/config": "workspace:*",
    "typescript": "^5.7.2"
  }
}
